name: Run server tests

on:
  workflow_call:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        uses: elisaalbertini/paguroDbRepo/.github/setupAction@::${GITHUB_REF#refs/*/}
      - name: Run tests
        uses: elisaalbertini/paguroDbRepo/.github/serverTest@::${GITHUB_REF#refs/*/}
      