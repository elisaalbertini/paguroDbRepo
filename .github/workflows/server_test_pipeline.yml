name: Run server tests

on:
  workflow_call:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        uses: elisaalbertini/paguroDbRepo/.github/setupAction@${{ github.caller_ref }}
      - name: Run tests
        uses: elisaalbertini/paguroDbRepo/.github/serverTest@${{ github.caller_ref }}
      